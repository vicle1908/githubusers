name: Enhanced CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  GRADLE_OPTS: -Dorg.gradle.jvmargs="-Xmx4g -XX:MaxMetaspaceSize=2g -XX:+UseG1GC"

jobs:
  android-toolchain:
    uses: ./.github/workflows/android-toolchain-versions.yml

  code-quality:
    name: Code Quality & Static Analysis
    runs-on: ubuntu-latest
    needs: android-toolchain
    env:
      ANDROID_API_LEVEL: ${{ needs.android-toolchain.outputs.android-api-level }}
      ANDROID_BUILD_TOOLS: ${{ needs.android-toolchain.outputs.android-build-tools }}
      ANDROID_NDK_VERSION: ${{ needs.android-toolchain.outputs.android-ndk-version }}
      ANDROID_CMAKE_VERSION: ${{ needs.android-toolchain.outputs.android-cmake-version }}
    timeout-minutes: 15

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Prepare Gradle environment
      uses: ./.github/actions/gradle-android-setup
      with:
        android-api-level: ${{ env.ANDROID_API_LEVEL }}
        android-build-tools: ${{ env.ANDROID_BUILD_TOOLS }}
        android-ndk-version: ${{ env.ANDROID_NDK_VERSION }}
        android-cmake-version: ${{ env.ANDROID_CMAKE_VERSION }}

    # No separate plugins build needed; root tasks cover this

    - name: Run Detekt
      run: ./gradlew detektAll --configuration-cache --build-cache

    - name: Run ktlint
      run: ./gradlew ktlintCheckAll --configuration-cache --build-cache

    - name: Run Android Lint
      run: ./gradlew :app:lintDebug --configuration-cache --build-cache

    # Check if we can compile core-security native library
    - name: Check core-security compilation
      run: ./gradlew :core-security:compileDebugKotlin --configuration-cache --build-cache
      continue-on-error: true

    - name: Upload Detekt results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: detekt-results-${{ github.run_number }}
        path: |
          **/build/reports/detekt/
        retention-days: 30
        compression-level: 6
        if-no-files-found: ignore

    - name: Upload Lint results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: lint-results-${{ github.run_number }}
        path: |
          **/build/reports/lint-results-*.html
        retention-days: 30
        compression-level: 6
        if-no-files-found: ignore

  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs:
      - android-toolchain
      - code-quality
    env:
      ANDROID_API_LEVEL: ${{ needs.android-toolchain.outputs.android-api-level }}
      ANDROID_BUILD_TOOLS: ${{ needs.android-toolchain.outputs.android-build-tools }}
      ANDROID_NDK_VERSION: ${{ needs.android-toolchain.outputs.android-ndk-version }}
      ANDROID_CMAKE_VERSION: ${{ needs.android-toolchain.outputs.android-cmake-version }}
    permissions:
      checks: write
      contents: read

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Prepare Gradle environment
      uses: ./.github/actions/gradle-android-setup
      with:
        android-api-level: ${{ env.ANDROID_API_LEVEL }}
        android-build-tools: ${{ env.ANDROID_BUILD_TOOLS }}
        android-ndk-version: ${{ env.ANDROID_NDK_VERSION }}
        android-cmake-version: ${{ env.ANDROID_CMAKE_VERSION }}

    - name: Run unit tests
      run: ./gradlew testAll --configuration-cache --build-cache

    - name: Generate test report
      if: always() && hashFiles('**/build/test-results/test*/TEST-*.xml') != ''
      uses: dorny/test-reporter@v1
      with:
        name: Unit Test Results
        path: '**/build/test-results/test*/TEST-*.xml'
        reporter: java-junit
        fail-on-error: false
        fail-on-empty: false
        max-annotations: '50'

    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: unit-test-results-${{ github.run_number }}
        path: |
          **/build/test-results/
          **/build/reports/tests/
        retention-days: 30
        compression-level: 6
        if-no-files-found: ignore
